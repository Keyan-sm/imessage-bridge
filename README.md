# imessage-bridge
Two-way bridge between macOS Messages (chat.db) and a Google Drive folder: read iMessages from inbox.json, send them via AppleScript from outbox.json. Runs as a launchd agent on an always-on Mac.
